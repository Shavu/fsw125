require() /module.exports

const sum = (a, b) => {
    return a + b
};

const sub = (c, d) => {
    return c - d
};

const mult = (e, f) => {
    return e * f
};

const divide = (g, h) => {
    return g / h
};

module.exports = {
    sum,
    sub,
    mult,
    divide
}

// const sum = (a, b) => {
//     return a + b
// };

// const sub = (c, d) => {
//     return c - d
// };

// const mult = (e, f) => {
//     return e * f
// };

// const divide = (g, h) => {
//     return g / h
// };

// module.exports = {
//     sum,
//     sub,
//     mult,
//     divide

// };